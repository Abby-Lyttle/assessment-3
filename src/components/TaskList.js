import  {React, useState }from 'react'
import { addTask } from '../state/taskList/taskListSlice';
import '../App.css';

function TaskList()  {
const AddTask => (props);
return(
    
   
    
        
        <div>
        <section className="task-container">hi
      
        <div>
        <button  onClick={() => addTask()}> To Do</button>
        <button  onClick={() => setAddTask()}> Complete</button>
        </div>
        </section>
        
        </div>
    
)
}

export default TaskList;
